@model DevelopersBuddyProject.ViewModel.QuestionViewModel
<tr>
    <td><span class="badge bg-secondary">@Model.VotesCount</span> Votes</td>
    <td><span class="badge bg-secondary">@Model.AnswersCount</span> Answers</td>
    <td><span class="badge bg-secondary">@Model.ViewsCount</span> Views</td>
    <td class="text-dark">
        <a class="text-decoration-none text-black" href="/questions/view/@Model.QuestionId">@Model.QuestionName</a><br />
        <span class="badge bg-secondary">@Model.Category.CategoryName</span>
    </td>
    <td>
        @if (Convert.ToInt32(Session["CurrentUserId"]) != Model.UserId)
        {
            @:by @Model.User.Name
        }
        else
        {
            @:by you
        }
    </td>
    <td>on @Model.QuestionDateAndTime</td>
</tr>